# Attribution

`browser-profiling.md` is adapted from the `optimize-web-animations` skill in
[MengTo/Skills](https://github.com/MengTo/Skills) — see that repository's LICENSE. The DOM /
animation / heap evaluator payloads are kept verbatim; only the browser-attach section was rewritten
to drive via the Playwright plugin / claude-in-chrome instead of Codex Browser.
