# Bug scan lens

Read the diff for logic errors, off-by-ones, null/nil issues, race conditions, resource leaks. Ignore style and nitpicks. Distinguish hard bugs from judgement calls.

Axis tag: `bug`.
